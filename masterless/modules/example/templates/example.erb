This is just an example of a file that would normally be pulled from puppet
master via TEMPLATE, yet now it is pulled from ./ without any manifest changes.

Template variables interpolated!

param1 <%= @param1 %>
param2 <%= @param2 %>
expath <%= @expath %>

