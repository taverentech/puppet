#######################################################
#  DNS server configs - dns, ldap

class dns {
  notice(">>> dns class <<<")

  if ($::environment == "dev") {
  } elsif ($::environment == "stage") {
  } elsif ($::environment == "prod") {
  }

}
