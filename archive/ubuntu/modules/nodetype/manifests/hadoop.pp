#######################################################
#  hadoop server configs

class hadoop {
  notice(">>> hadoop class <<<")

  if ($::environment == "dev") {
  } elsif ($::environment == "test") {
  } elsif ($::environment == "stage") {
  } elsif ($::environment == "prod") {
  }

}
